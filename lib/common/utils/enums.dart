enum QueryType { all, popular, traditional, men, women, kids }

enum WishlistToggle {
  retrieve,
  delete,
  add,
}

enum FetchOrdersTypes {
  pending,
  delivered,
  cancelled,
}

enum ReloadCount {
  delete,
  reload,
  update,
}

enum NotificationCount {
  update,
  reload,
}
